#include <stdio.h>
/**
 * main - funtion to print the name of the function
 * @argc: argument count
 * @argv: argument vector
 * Return: return 0
 */
int main(int __attribute__((unused)) argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
